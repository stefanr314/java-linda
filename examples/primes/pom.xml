<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>rs.ac.bg.etf.kdp</groupId>
        <artifactId>examples</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>primes</artifactId>
    <packaging>jar</packaging>

    <name>java-linda :: examples :: primes</name>
    <description>
        Sample user job: seeds candidate tuples and distributes primality
        checks across workstations via eval(), written only against the
        Linda interface.
    </description>

    <dependencies>
        <dependency>
            <groupId>rs.ac.bg.etf.kdp</groupId>
            <artifactId>common</artifactId>
        </dependency>
        <dependency>
            <groupId>rs.ac.bg.etf.kdp</groupId>
            <artifactId>linda-client</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <configuration>
                    <transformers>
                        <transformer
                                implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <mainClass>rs.ac.bg.etf.kdp.examples.primes.PrimesMain</mainClass>
                        </transformer>
                    </transformers>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>